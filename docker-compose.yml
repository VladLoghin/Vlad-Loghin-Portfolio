services:
  db:
    image: postgres:16-alpine
    volumes:
      - pgdata:/var/lib/postgresql/data

  backend:
    build: ./vladportfolio-backend
    depends_on:
      - db

  frontend:
    depends_on:
      - backend

volumes:
  pgdata:
